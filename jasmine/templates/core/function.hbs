<div class="function-header">
  <em>{{class}}</em>
  <h2 class="function-name">{{name}} <small>({{#each arguments}}{{#if @index}}, {{/if}}{{this.name}}{{/each}})</small></h2>
</div>
<div class="function-body">
  <p>{{description}}</p>
</div>
